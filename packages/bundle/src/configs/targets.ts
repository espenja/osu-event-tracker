// https://babeljs.io/docs/en/babel-preset-env#targets

/**
 * Babel preset-env targets for browsers
 */
export const BROWSER_TARGETS =
	"last 1 version, not ie > 0, not op_mob > 0, not ie_mob > 0, not op_mini all"

/**
 * Babel preset-env targets for node
 */
export const NODE_TARGETS = {
	node: 14
}
